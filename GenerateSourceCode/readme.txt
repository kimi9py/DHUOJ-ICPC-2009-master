First,set new password for root:mysql>set password = password('root');

Second,java -jar H:\WorkSpace\04-GenerateSourceCodes\dist\GenerateSourceCodes.jar 100,

	then the sourceCodes will be generated in the current directory.